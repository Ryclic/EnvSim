[project]
name = "envsim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT AND (Apache-2.0 OR BSD-2-Clause)"
dependencies = [
    "pygame>=2.6.1",
]
